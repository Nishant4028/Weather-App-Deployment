trigger:
  none  # Manual run only for testing

pool:
  name: az-devagent
  demands:
  - agent.name -equals az-agent1  # Replace with your actual agent pool name

jobs:
- job: HelloWorldJob
  displayName: 'Test Self-Hosted Agent'
  steps:
    - script: echo "✅ Hello, Nishant! Your agent is working!"
      displayName: 'Say Hello'